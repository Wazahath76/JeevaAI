package com.medicore.hms.config;

/**
 * Kafka config disabled for local dev.
 * Restore full implementation from medicore-response4.zip when Kafka is available.
 */
public class KafkaConfig {
    // No beans — Kafka autoconfiguration excluded in application.yml
}
